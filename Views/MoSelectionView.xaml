<Page x:Class="dapdon.Views.MoSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="EPC Viewer"
 Width="200"
 Height="300"
      >

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Chọn MO:" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding MoList}" SelectedItem="{Binding SelectedMo}" />
            <Button Content="OK" Command="{Binding ConfirmCommand}" Margin="10" />
        </StackPanel>
    </Grid>
</Page>
