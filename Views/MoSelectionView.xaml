<Window  x:Class="dapdon.Views.MoSelectionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="EPC Viewer"
 Width="400"
 Height="200" 
       WindowStartupLocation="CenterOwner"
      >

    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
             Width="150" Height="30" 
             KeyUp="ComboBox_KeyUp"
            />
            <ComboBox ItemsSource="{Binding FilteredMoNoList}" 
              SelectedItem="{Binding SelectedMoNo, Mode=TwoWay}" 
              Width="200" Height="30"/>
        </StackPanel>


        <Button Content="Confirm" Command="{Binding ConfirmCommand}" 
            Margin="10" Width="100" Background="#31d097" Foreground="Black" Height="40">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </StackPanel>

</Window>
